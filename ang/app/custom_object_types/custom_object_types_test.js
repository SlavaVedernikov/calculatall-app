'use strict';

describe('myApp.custom_object_types module', function() {

  beforeEach(module('myApp.custom_object_types'));

  describe('custom_object_types controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var custom_object_types_Ctrl = $controller('custom_object_types_Ctrl');
      expect(custom_object_types_Ctrl).toBeDefined();
    }));

  });
});