'use strict';

describe('myApp.objects module', function() {

  beforeEach(module('myApp.objects'));

  describe('objects controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var objects_Ctrl = $controller('objects_Ctrl');
      expect(objects_Ctrl).toBeDefined();
    }));

  });
});