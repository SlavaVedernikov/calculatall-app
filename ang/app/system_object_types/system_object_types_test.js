'use strict';

describe('myApp.system_object_types module', function() {

  beforeEach(module('myApp.system_object_types'));

  describe('system_object_types controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var system_object_types_Ctrl = $controller('system_object_types_Ctrl');
      expect(system_object_types_Ctrl).toBeDefined();
    }));

  });
});